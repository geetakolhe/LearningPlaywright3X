// ## Ternary (conditional) Operator
// condition, either this or either that.
let age = 20;
let is_geet_will_go_to_goa = age > 18 ? "yes" : "no";
console.log(is_geet_will_go_to_goa);

// Condition ? value(if true) : value (if else)
  