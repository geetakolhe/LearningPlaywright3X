let status = 0;
console.log(typeof status);
  switch (status){
    case false:
      console.log("false matched ");
      break;
    case 0:
      console.log("0 matched");
      break;   

  }

  // === Strict, value and data type of them should of them should be same
  // && -> and gate
  // || -> Or gate
  // & - bitwise, | bitwise, never use
  // == -> loose comparison - value or data type